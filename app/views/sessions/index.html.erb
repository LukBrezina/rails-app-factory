<section class="flex-1 flex items-center justify-center p-8 text-center">
  <div class="max-w-md">
    <div class="text-[10px] tracking-[0.3em] text-faint mb-3">SETTING UP</div>
    <p class="text-[13px] text-idle m-0"><%= @app.display_name %> is still being set up on this box — give it a minute, then refresh.</p>
  </div>
</section>
